language: node_js
node_js:
- stable
cache:
  directories:
  - node_modules
before_script:
- npm install -g yarn
- cp ./src/example.config.js ./src/config.js
install:
- yarn
script:
- if [ "TRAVIS_PULL_REQUEST" = "false" ]; then bash ./scripts/decrypt.sh; fi
- yarn build
- yarn lint
- yarn test
